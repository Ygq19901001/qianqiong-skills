# -*- coding: utf-8 -*-
"""公文排版共享样式常量（build 与 verify 共用，避免字体名/字号错位）。

字体选型依据本机实测（C:/Windows/Fonts）：
  - 方正小标宋简体：缺失（商业字体），改用 华文中宋(STZHONGS) —— 同属宋体类标题字，最接近小标宋外观。
  - 仿宋_GB2312 / 楷体_GB2312：老 GB2312 编码字体，字符集受限且易被替换，改用 Unicode 版 仿宋 / 楷体。
  - 黑体 / 宋体 / 华文中宋：本机均存在。
字号换算：初号=42pt，小初=36pt，二号=22pt，三号=16pt，四号=14pt。
首行缩进：firstLine twips = 字符数 × 字号pt × 20（1pt=20twips，汉字宽≈字号）。
"""
FONT_XBS = "华文中宋"      # 小标宋替代（红头/标题/附件标题）—— 本机无方正小标宋，华文中宋最接近
FONT_FS  = "仿宋"          # 仿宋（正文/署名/日期/附件说明/名单）—— 用 Unicode 版
FONT_HEI = "黑体"          # 黑体（一级标题/附件标签）
FONT_KAI = "楷体"          # 楷体（二级标题）—— 用 Unicode 版
FONT_SONG = "宋体"         # 宋体（页码）
RED = "FF0000"
PT3 = 16   # 三号
PT2 = 22   # 二号
PT1 = 42   # 初号（红头）
PT4 = 14   # 四号（页码）
FIRSTLINE_COEF = 20  # twips per (字符数 × 字号pt)
